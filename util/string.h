#include <string>

using namespace std;

// bool string_includes(string str, string substr) {
// 	int i = -1;
// 	int len = substr.length();

// 	while (str[i++] && i + len < str.length()) {
// 		if (str.substr(i, len) == substr) {
// 			return true;
// 		}
// 	}

// 	return false;
// }

bool string_includes(string str, string subStr);